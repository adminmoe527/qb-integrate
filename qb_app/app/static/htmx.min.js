// htmx.min.js — replaced at build time with the real htmx 1.9.x minified bundle.
// Download from https://unpkg.com/htmx.org@1.9.12/dist/htmx.min.js and place here.
// This stub exists so the scaffolding is complete; the installer's build step
// fetches the real file into the wheelhouse bundle.
console.warn("htmx stub loaded — replace with real htmx.min.js before shipping.");
